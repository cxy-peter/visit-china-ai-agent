# V6.6｜发布与实际验收

2026-09-25。基于已合并V6.5 `a883f7c9`。这次为应用和产品增量，不训练外部模型权重，不移植示例项目的组件或数字。

## 已完成的验收

应用提交 `e0b180637f221938b37a84ec72d89827119d8b9d` 在 [CI 36181133395](https://github.com/cxy-peter/visit-china-ai-agent/actions/runs/36181133395) 完整通过：

| 范围 | 实际结果 |
|---|---:|
| 原提示词与模型预算/模块契约 | 23/23 |
| 程序与接口 | 455/455，0失败、0跳过 |
| 新服务与运营真实浏览器 | 24/24 |
| 五城KB与运营兼容浏览器 | 29/29 |
| V6.4关键交互兼容浏览器 | 19/19 |

程序套件包含原20,000条合成/资料改写回归。浏览器使用实际Chromium和本地HTTP，上游为受控fixture，未进行新付费模型调用或真人麦克风试验。多套检查可能重叠，不把总数视为独立问题、用户或语义准确率。

工件 `v66-validation-evidence` 中有 `v6.6/acceptance.json`、程序日志、23项保留报告、各浏览器报告及截图；源码工件为 `v66-verified-source`。后续仅同步侧栏V6.6标签与说明文字，最终PR必须重新检查当前提交；不把历史成功自动当作新提交成功。

## 中断恢复与真实失败

1. 首轮 `36177670501` 程序452项通过，浏览器重验收按钮位于折叠候选中，实际不可见。改成最新待审候选默认展开，并断言可见；没有force click。
2. 发现仅选上午时表格与请求参数不同，新增共用时段交集；矛盾区间拒绝，时段单独选择也发送正确范围。
3. 恢复运行 `36180515145` 新浏览器24项和五城29项通过，旧兼容因为“一日团”文案变成“一日游”失败；保留原用户术语后完整19项兼容通过。没有删除旧测试。
4. 显式选项不变成预订；新聊天清理标签页选择；接口15秒超时保留原对话，候选资料指纹补充来源URL和类型。

## 保留的能力

原意图提示词7,123字符及V6.5五城补充保持，原生成/提取/改进提示、DeepSeek适配、模型路由/预算、语音和既有行为审核模块通过保留检查。新增卡片不增加模型调用，标准KB仍仅限登记问法，复杂问题继续原模型链路。

这证明代码和契约保留，不承诺所有真实回答零退化。人工标注真实语义、真人语音、服务商库存及生产部署需另外验证；未以离线检查代替业务验收。

## 范围与下一步

已经实现情境服务卡、模拟查询、铁路时段保存、统一反馈、目标预设、共同候选验证、指纹、重验收和人工发布/回滚。未实现任意代码自改、真实订单、随机在线灰度与自动收益回滚；五账号规则仍只用于原对应行为策略。

方法、数据与接口详见[技术交接](V6_6_IMPLEMENTATION_AND_PRODUCT.md)。更新本地前保留.env、运行数据与私人账号文件。GitHub合并与Vercel部署是不同状态。
