# 免密码旅行聊天：生产复验与需求澄清修正

## 已确认的生产事实

PR #6 合并提交 `1bf13924a960f440dea64abe9821144852f2d009` 已通过原有四套CI并由现有GitHub—Vercel链路发布。生产检查36205920707核对了正式域名的sourceCommit与app.js哈希；新访客公开状态为publicAccess/authorized/configured=true，页面打开没有模型请求。Operations仍是独立鉴权。

同次复验中的一条真实问题要求“先了解旅行需求，再做行程”，等待37.29秒后收到ANSWER_CITATION_REQUIRED。这证明还存在回答流程问题，不能把绿色部署说成真实回答已通过，也不能再归因于访客密码。

## 本次最小修改

增加 `v5/planning-clarification.js`：仅识别明确要求先问需求/偏好的中英文输入。继续使用原有模型意图调用，模型文字只选择日期、预算、节奏等批准问句主题；最终展示至多三条受限问题。已确认的同行人、兴趣和预订状态不会重新询问。原始模型文字不能直接成为政策、数值、预订或链接断言。

此分支标记为`deepseek-clarification` / `conversation-only`，不挂虚假的官网引用。涉及签证、费用、票务、资格或多个独立任务的请求仍使用原有检索、生成和校验。普通“生成一份行程”也不由这个问句目录替代。

原PROMPT、模型模式、输出预算、数字与来源校验没有删改。此修改不是全面放宽无引用回答，也不是更换或微调DeepSeek。

## 验收

新增中英文、混合事实、恶意模型文字、已知状态、多任务保留和原校验不变的程序测试。原免密码浏览器测试继续对一般请求运行真实HTTP和有来源的模型fixture，不能仅验证通用兜底。

合并后使用原来失败的同一个生产问题复验，不改成更容易通过的另一个问题；每次发布验证最多一条真实提交，没有付费自动重试。完整结果和失败由public-chat-production-verification工件记录。单题连通成功也不等于整体答案质量、语音、订单或业务效果已验收。

公开聊天消耗项目配置的模型额度。现有进程级限流保留，但不是持久化全站费用上限。设`TRAVEL_CHAT_PUBLIC=0`并重新部署可恢复私人聊天；Operations权限从未开放给匿名用户。
